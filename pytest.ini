[pytest]
testpaths = test
addopts = --basetemp=runs/tmp/pytest
markers =
    unit: fast unit tests
    integration: multi-module integration tests
    slow: long-running tests (training/inference smoke)
    gpu: tests that require GPU
